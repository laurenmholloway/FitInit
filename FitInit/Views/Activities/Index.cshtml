@model IEnumerable<FitInit.Models.Activity>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ActivityName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ActivityReps)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ActivityUser)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Goal.GoalName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ActivityName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ActivityReps)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ActivityUser)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Goal.GoalName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ActivityID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ActivityID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ActivityID })
        </td>
    </tr>
}

</table>
